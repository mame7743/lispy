# .pre-commit-config.yaml
repos:
- repo: local
  hooks:
    - id: commitizen-check
      name: Commitizen check
      entry: cz check --commit-msg-file
      language: system
      stages: [commit-msg]
      pass_filenames: true